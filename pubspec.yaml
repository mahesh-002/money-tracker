name: moneytracker
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

  intl: any  # Ensure intl is correctly placed

  flutter_screenutil: ^5.9.0  # Corrected placement, removed nested dependencies
  flutter_material_pickers: ^3.6.0  # Check for the latest version on pub.dev


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
